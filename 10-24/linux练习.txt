uname -r 查看内核版本,判断软件和内核是否兼容

yum -y install 
yum -y remove 
yum search cc|grep cc

yum update 

vim 
/     文件中查找

在当前目录下查找文件
	find -name linux练习.txt
查找目录(文件夹)
	find -name '10-24'
修改文件名
	mv a.txt b.txt
	
